extends layout

block content
    ul.menu
        each user in users
            li.menu__item                    
                a(href='/user-' + user.id ) #{user.name}
    p.centre
        a(href='/stats') Stats
