extends layout

block content
    h1.centre Pick a player
    ul.menu
        each user in users
            li.menu__item                    
                a(href='/user-' + user.id ) #{user.name}
