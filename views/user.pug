extends layout

block content
    h1= user.name
    p What did you do?
    ul
        each job in jobs
            li                    
                a(href='/user-' + user.id + '/job-' + job.id ) #{job.title}
