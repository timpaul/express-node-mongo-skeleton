extends layout

block content
    h1.centre #{user.name}
    ul.menu
        each job in jobs
            li.menu__item
                .points #{job.points}                   
                a(href='/user-' + user.id + '/job-' + job.id ) #{job.title}
